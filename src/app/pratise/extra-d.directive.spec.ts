import { ExtraDDirective } from './extra-d.directive';

describe('ExtraDDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtraDDirective();
    expect(directive).toBeTruthy();
  });
});
